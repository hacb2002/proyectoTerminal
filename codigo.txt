int g(int a, int b, int c, int d, int e){
    return 0;
}

int ejemplo_operadores(int x, int y,) {
    /* Declaración de variables */
    int a = 10; // literal entera y asignación
    int b = 5;  // otra asignación
    int c = a, d = c, resultado = a, f = f; // uso de coma en declaración
    /*
    Hola mundo
    */
    /*
    Este es otro comentario de linea. 
    */
    //Hola
    // Operaciones aritméticas
    resultado = a + b;
    g(1,2,3,a,resultado);
    resultado = a - b;
    resultado = a, resultado = b, resultado = resultado;
    resultado = a * b;
    resultado = a / b;
    resultado = a % b;
    a+b;
    a+resultado;
    // Comparaciones y lógica
    if (a < b) {
        resultado = 1;
    } else if (a <= b) {
        resultado = 2;
    } else if (a > b) {
        resultado = 3;
    } else if (a >= b) {
        resultado = 4;
    } else if (a == b) {
        resultado = 5;
    } else if (a != b) {
        resultado = 6;
    }
    // Lógica booleana con AND y OR
    if ((a > 0) && (b > 0)) {
        resultado = 7;
    }

    if ((a > 0) || (b < 0)) {
        resultado = 8;
    }

    // Negación lógica
    if (!(a == b)) {
        resultado = 9;
    }

    // Uso de llaves, paréntesis y punto y coma ya está en todo el cuerpo
    return resultado; // retorno final
    /*
    Hola a todos;
    */
}/*
Comentario sin sentido
*//*comentario sin sentido al final del archivo*/
//ultimo comentario. 
